export enum Status {
  Failed = "failed",
  Idle = "idle",
  Loading = "loading",
  ShouldUpdate = "should_update",
}

export enum Size {
  Large = "large",
  Medium = "medium",
  Small = "small",
}

export enum Toggle {
  Disabled,
  Enabled,
}
